\documentclass[11pt,a4paper]{article}
% ============================
% PACKAGES
% ============================
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{bm}
\usepackage{physics}
\usepackage{geometry}
\usepackage{hyperref}
\usepackage{graphicx}
\usepackage{cite}
\usepackage{authblk}
\usepackage{setspace}
\usepackage{titlesec}
\usepackage{listings}
\usepackage{xcolor}
\geometry{margin=1in}
% ============================
% TITLE FORMAT
% ============================
\titleformat{\section}{\Large\bfseries}{\thesection.}{0.6em}{}
\titleformat{\subsection}{\large\bfseries}{\thesubsection}{0.6em}{}
% ============================
% TITLE
% ============================
\title{FRACTAL SPATIO-TEMPORAL GRAVITY (FSG)}
\author{Laurent MARIE-LOUISE}
\date{Version 6.1 --- July 2026}
% ============================
\begin{document}
\maketitle
\begin{onehalfspacing}
% ============================
% ABSTRACT
% ============================
\begin{abstract}
We present a corrected formulation of Fractal Spacetime Gravity (FSG), a framework in which dark matter is replaced by an infrared modification of the effective dimensionality of spacetime. The founding principle (the ``pillar'') is that the gravitational flux propagates through an effective number of spatial dimensions $d_{\rm eff}$ that flows from $3$ in the high-acceleration regime to $2$ in the deep infrared, with the transition set by the acceleration scale $a_0$. Flux conservation, $g\,A(r)=4\pi G M$ with $\mathrm{d}\ln A/\mathrm{d}\ln r = d_{\rm eff}-1$, then yields for a point mass the exact deep-MOND law $g=\sqrt{g_N a_0}$, the logarithmic potential, flat rotation curves, and the \emph{exact} baryonic Tully--Fisher relation $V^4=GM a_0$ (verified numerically to $<0.01\%$). We show that the observed radial acceleration relation is itself a direct measurement of the function $d_{\rm eff}(g)$. Because the reduction is geometric ($\Phi=\Psi$), gravitational lensing follows the dynamics automatically, evading the historical failure of scalar MOND theories.

We emphasize the epistemic status honestly: the pillar is an \emph{effective law}, at the same level of description as Milgrom's 1983 formulation, and the covariant action realizing it is stated as the central open problem. Two earlier claims to derive this law from a covariant action --- the $k^{-3}$ propagator argument of v5.1 and the kinetic/curvature non-local reductions of the v7 draft --- are shown to fail by explicit no-go results and are formally retracted.

The acceleration scale is fixed by the cosmic memory fields $U=\Box^{-1}R$, $V=\Box^{-2}R$: $a_0 = cH_0\,|V_0|^{3/2}/|U_0| \simeq 1.14\times10^{-10}\,{\rm m/s^2}$, within $5\%$ of observation with no free parameter. The same memory sector drives a phantom-like dark energy ($w_0<-1$). Solar System tests are satisfied without screening, and in the strong field Schwarzschild/Kerr are recovered up to $\mathcal{O}(e^{-g/a_0})$.

A new result of this corrected version concerns \emph{galaxy clusters}: the residual mass discrepancy of MOND in cluster cores (a factor $2$--$3$) is resolved by a \emph{memory modulation} of the acceleration scale, $a_0^{\rm eff}=a_0\,[1+(\delta U/U_*)^n]$ with $\delta U=2|\Phi|/c^2$. A single parameter $U_*\simeq 4.5\times10^{-5}$ closes the deficit across the full 12-cluster X-COP sample (residual $0.10$~dex, at the intrinsic scatter floor), leaves galaxies untouched ($<0.004$~dex), and agrees to $\sim10\%$ with the independent lensing calibration of CLASH clusters.

We are explicit about what remains open: (i) the covariant action for the pillar; (ii) the spatial lensing offset of the Bullet Cluster, which we show is \emph{not} produced by the memory mechanism --- the same wall faced by all modified-gravity approaches --- and remains unsolved; and (iii) the CMB acoustic-peak amplitudes, which require a dedicated non-local Boltzmann code. The CMB peak \emph{geometry} is shown compatible with FSG. This version (6.1) additionally records concrete progress on the central open problem (Section~\ref{sec:aest}): the single-potential structure of the pillar identifies its covariant home as the Aether--Scalar--Tensor (AeST) class, whose free function is fixed by $d_{\rm eff}(g)$ and whose dark sector is carried by the same memory field that fixes $a_0$. These are structural correspondences, not a completed derivation, and the honest boundaries are stated.
\end{abstract}
\newpage
\tableofcontents
\newpage
% ============================================================
\section{Introduction}
% ============================================================
The standard cosmological model $\Lambda$CDM successfully accounts for a wide
range of observations but at the cost of introducing two hypothetical
components: dark matter (26\% of the energy budget) and dark energy (69\%),
neither detected in laboratory experiments.
At galactic scales, the dark matter paradigm faces well-established empirical
regularities:
\begin{itemize}
    \item the baryonic Tully--Fisher relation (BTFR),
    \item the radial acceleration relation (RAR),
    \item the diversity problem,
    \item the existence of ultra-thin galactic disks,
    \item early massive galaxies at high redshift in JWST observations.
\end{itemize}
Modified Newtonian Dynamics (MOND) reproduces BTFR and RAR with remarkable
precision but historically lacked: (i) a covariant formulation,
(ii) a natural cosmological extension, and (iii) full consistency with the CMB.
Fractal Spacetime Gravity (FSG) addresses these issues by replacing dark matter
with a modification of the \emph{infrared geometry} of spacetime, encoded in a
non-local action that dynamically reduces the effective spectral dimension of
spacetime from $d_S=4$ to $d_S \simeq 2$ at large scales.
% ============================================================
\section{Motivations for Infrared Fractality}
% ============================================================
Dimensional reduction is a ubiquitous prediction of several approaches to
quantum gravity, including:
\begin{itemize}
    \item Causal Dynamical Triangulations (CDT),
    \item Asymptotic Safety,
    \item Hořava--Lifshitz gravity,
    \item Loop Quantum Gravity.
\end{itemize}
These frameworks typically predict:
\[
d_S(k\to\infty) \to 2,
\]
i.e.\ a reduction of effective dimensionality in the ultraviolet.
In FSG we reverse the paradigm: the reduction occurs in the infrared (IR),
motivated by holographic considerations and by the causal structure of
de Sitter spacetime. This IR fractality modifies long-wavelength propagation of
curvature exactly at the scales where dark matter phenomenology appears.
% ---------------- FIGURE ----------------
\begin{figure}[h!]
\centering
\includegraphics[width=0.65\textwidth]{fig_dimension_spectral.pdf}
\caption{
Illustrative running of the spectral dimension as a function of energy scale.
In FSG the dimension increases in the UV but approaches $d_S\simeq 2$ in the IR.
}
\label{fig:dimension}
\end{figure}
% ============================================================
\section{The Non-Local Effective Action}
% ============================================================
We introduce the non-local scalar field
\[
X = \Box^{-1}R ,\qquad \Box X = R,
\]
with the retarded Green function ensuring causality.
The FSG action is defined to impose a dimensional reduction in the infrared:
\begin{equation}
S = \frac{M_P^2}{2} \int d^4x\,\sqrt{-g}\;
R\bigl[1 + f(X)\bigr] + S_m .
\label{eq:action_fsg}
\end{equation}
The function $f(X)$ is constrained by the requirement that the graviton propagator must scale as $k^{-3}$ in the deep IR to recover flat rotation curves. Dimensional analysis and renormalization group flow arguments suggest a fractional power law form:
\begin{equation}
f(X) = \gamma \left( \frac{L^2}{X} \right)^{\frac{1}{2}} ,
\label{eq:fractional_f}
\end{equation}
where $L \sim 1/\sqrt{\Lambda}$ is the cosmic horizon scale and $\gamma$ is a dimensionless coupling constant of order unity.
This fractional form $X^{-1/2}$ is the signature of a fractal geometry where the measure of spacetime scales non-trivially. Unlike logarithmic corrections which only produce mild running couplings, this power-law correction enforces a hard transition in the spectral dimension.
% --- [PATCH 1: LOCALIZATION] ---
\subsection{Localization of the Action}
To study degrees of freedom and stability one can localize the non-local operator by introducing two scalar auxiliary fields $U$ and $\xi$ enforcing $U=\Box^{-1}R$.
A convenient localized action equivalent (on-shell) to Eq.~\eqref{eq:action_fsg} is
\begin{equation}
S_{\rm loc}=\frac{M_P^2}{2}\int d^4x\sqrt{-g}\left\{R\bigl[1+f(U)\bigr]+\xi\bigl(\Box U-R\bigr)\right\}+S_m.
\label{eq:Sloc}
\end{equation}
Variation with respect to the metric gives the modified Einstein equations.
\newpage
% ============================================================
\section{Variation of the Action and Field Equations}
% ============================================================
Starting from the action
\begin{equation}
S = \frac{M_P^2}{2}\int d^4x\,\sqrt{-g}\,
R\,[1+f(X)] + S_m ,
\qquad X = \Box^{-1} R ,
\label{eq:action2}
\end{equation}
we compute the metric variation. The non-local variation is:
\begin{equation}
\delta X = -\Box^{-1}(\delta\Box)\,X + \Box^{-1}(\delta R).
\label{eq:deltaX}
\end{equation}
The operator $\Box^{-1}$ is defined with the \emph{retarded} Green function,
ensuring:
\begin{itemize}
    \item no violation of causality,
    \item no superluminal propagation,
    \item absence of Ostrogradsky instabilities (unlike naïve $f(R)$ models).
\end{itemize}
The modified Einstein equations take the form
\begin{equation}
G_{\mu\nu} + \Delta G_{\mu\nu}
= 8\pi G\,T_{\mu\nu},
\label{eq:einstein_modified}
\end{equation}
with
\begin{align}
\Delta G_{\mu\nu}
&= f(X) G_{\mu\nu}
 + \left(\nabla_\mu \nabla_\nu - g_{\mu\nu}\Box\right) f(X)
 - \frac{1}{2} g_{\mu\nu} R\, f(X)
 + K_{\mu\nu}[X],
\label{eq:DeltaG}
\end{align}
where $K_{\mu\nu}[X]$ collects the variations of the non-local kernel
$\Box^{-1}$. In the linear regime, this reduces to the structure derived by
Deser--Woodard and Maggiore--Mancarella.
% ============================================================
\section{Physical Structure: The Inertia of the Vacuum}
% ============================================================
The non-local kernel $\Box^{-1}$ implies that the gravitational field is not merely a slave to the matter distribution (as in Poisson's equation $\nabla^2 \Phi = \rho$), but a dynamical entity governed by a wave-like equation.
The modified Einstein equations can be recast in the form of a driven wave equation for the scalar degree of freedom $\phi$:
\begin{equation}
\left( \frac{1}{c^2}\partial_t^2 - \nabla^2 + m_{\rm eff}^2 \right) \phi = 8\pi G T_{\mu\nu}^{\rm bar}.
\label{eq:wave_eqn}
\end{equation}
\subsection{The Gravitational Wake Effect}
This hyperbolic structure introduces a fundamental physical property: \textbf{Gravitational Inertia}.
Unlike the Newtonian potential which relaxes instantaneously, the FSG field carries momentum.
In quasi-static systems (isolated galaxies), the field has time to equilibrate, creating the "Dark Matter" halo effect centered on the baryons.
However, in highly non-equilibrium events such as high-velocity cluster collisions, the field dynamics decouple from the source.
The non-local memory of the spacetime geometry acts as a flywheel. When the baryonic component (gas) undergoes sudden hydrodynamic braking, the gravitational eigenmodes continue their trajectory ballistically. This creates a transient offset between the visible mass and the center of the potential well, explaining the "Dark Matter" signal in the Bullet Cluster without introducing new particles.
% ============================================================
\section{The Infrared Propagator: Target, Fourier Dictionary, and a Retraction}
% ============================================================
To determine the gravitational force law, we consider a weak perturbation around Minkowski spacetime:
\[
g_{\mu\nu} = \eta_{\mu\nu} + h_{\mu\nu}.
\]
Before deriving the force law, we state the target in Fourier language. The effective static propagator required for MOND dynamics is
\begin{equation}
G(k) \xrightarrow{\ \rm IR\ } \frac{1}{L\,k^{3}},
\label{eq:Gk3}
\end{equation}
i.e.\ an anomalous infrared exponent $\eta = 1$ relative to the Newtonian $1/k^2$. This correspondence ($\Phi \sim \ln r \Leftrightarrow G \sim k^{-3}$ in $d=3$) is an exact Fourier-transform statement; $\eta = 1$ is therefore a \emph{name} for the deep-MOND behaviour, not an independent hypothesis.
\subsection{Why a Naive Linearization Fails}
Earlier versions of this work attempted to obtain Eq.~\eqref{eq:Gk3} by a direct scaling substitution, writing $X \sim R/k^2$ and hence $f(X_k) \sim k$. We retract that argument: $X = \Box^{-1}R$ is \emph{dimensionless}, so it cannot scale as $1/k^2$; moreover, on an exact Minkowski background $\bar{R} = 0$ implies $\bar{X} = 0$, where $f(X) \sim X^{-1/2}$ is singular, so no perturbative expansion of $f$ around flat space exists. The propagator of the theory must instead be obtained from the second-order action around a cosmological (de Sitter-like) background where $\bar{X} = \mathcal{O}(10)$ is finite (see the memory-field integration of Section~\ref{sec:memory} for the numerical background values).
\subsection{Two Retractions and the Honest Logical Structure}
Two attempts to \emph{derive} the deep-MOND law from a covariant action are retracted in this corrected version:
\begin{itemize}
  \item the v5.1 propagator argument ($X\sim R/k^2 \Rightarrow f\sim k \Rightarrow G\sim k^{-3}$), invalid because $X=\Box^{-1}R$ is dimensionless and singular on a flat background;
  \item the reductions attempted in the v7 draft --- both a curvature-memory term $R\,f(\Box^{-1}R)$ and a kinetic-memory term $\propto(\partial\Box^{-1}R)^2$. Explicit quasi-static reduction shows the former yields a \emph{potential}-dependent, not acceleration-dependent, modification (rotation curves stay Keplerian), and the latter produces AQUAL only in a one-potential toy sector but fails the two-potential (lensing) reduction. Neither closes.
\end{itemize}
We therefore adopt the honest position of the present paper: FSG is founded on an \emph{effective principle} --- the infrared reduction of the gravitational flux from three to two effective dimensions (next section) --- at the same epistemic level as Milgrom's 1983 formulation of MOND. The spectral-dimension flow $d_S\to2$ is the \emph{motivation} for this principle, not a derivation of it; and the covariant action that realizes it is the central open problem (Section~\ref{sec:risks}, \ref{sec:future}).
% ---------------- FIGURE ----------------
\begin{figure}[h!]
\centering
\includegraphics[width=0.65\textwidth]{fig_propagator.pdf}
\caption{
Comparison of the Newtonian propagator $1/k^2$ with the FSG infrared
propagator $1/k^3$ (required for MOND dynamics).}
\label{fig:propagator}
\end{figure}
% ============================================================
\section{The Pillar: Infrared Dimensional Reduction of the Gravitational Flux}
\label{sec:pillar}
% ============================================================
The founding principle of FSG is stated directly, as an effective law. \textbf{Gravitational flux propagates through an effective number of spatial dimensions $d_{\rm eff}$ that flows from $3$ at high acceleration to $2$ in the deep infrared,} the transition being governed by the single acceleration scale $a_0$. This is the geometric content of ``infrared fractality'': at low accelerations spacetime behaves, for the purpose of flux propagation, as if two-dimensional.
\subsection{Flux Conservation and the Exact Deep-MOND Law}
Let $A(r)$ be the effective area through which the flux of a source of enclosed mass $M(r)$ passes. Flux conservation and the running of the effective dimension read
\begin{equation}
g(r)\,A(r) = 4\pi G\,M(r), \qquad \frac{\mathrm{d}\ln A}{\mathrm{d}\ln r} = d_{\rm eff}(g) - 1 .
\label{eq:flux}
\end{equation}
For $d_{\rm eff}=3$ (high acceleration) one has $A=4\pi r^2$ and Newton is recovered exactly. For $d_{\rm eff}\to 2$ (deep IR) the area grows as $A\propto r$: the flux spreads over a circle rather than a sphere. Taking the transition to occur where the Newtonian acceleration crosses $a_0$, the deep-IR branch gives, for a point mass,
\begin{equation}
g(r) = \sqrt{g_N(r)\,a_0} = \frac{\sqrt{GM a_0}}{r},
\label{eq:deepmond}
\end{equation}
i.e.\ Milgrom's law, the logarithmic potential $\Phi = \sqrt{GMa_0}\,\ln r$, and flat rotation curves. Circular orbits then satisfy $v^4 = GM a_0$: the \emph{exact} baryonic Tully--Fisher relation, with both slope and normalization fixed and no free parameter per galaxy. Numerical integration of \eqref{eq:flux} with a sharp transition confirms $v_\infty=(GMa_0)^{1/4}$ to better than $0.01\%$ over $10^8$--$10^{11}\,M_\odot$ (script \texttt{simulations/sim\_pillar\_flux.py}).
\subsection{The Radial Acceleration Relation Measures $d_{\rm eff}(g)$}
The only freedom in the pillar is the shape of the transition $d_{\rm eff}(g)$, exactly as the interpolating function $\mu$ is the only freedom in MOND. For a point source, Eq.~\eqref{eq:flux} is invertible: given the observed relation $g_{\rm obs}(g_{\rm bar})$, one reads off
\begin{equation}
d_{\rm eff}(g) = 1 - \frac{\mathrm{d}\ln g_{\rm obs}}{\mathrm{d}\ln r}.
\end{equation}
Applying this to the empirical radial acceleration relation (RAR) of McGaugh--Lelli--Schombert yields a function that flows monotonically from $3$ to $2$ with the transition at $g\simeq a_0$; reinjecting it into \eqref{eq:flux} reproduces the RAR by construction (a consistency check, not an independent prediction). \textbf{The observed RAR is thus a direct measurement of the dimensional flow of the gravitational flux.} A smooth choice such as $d_{\rm eff}(g)=3-e^{-\sqrt{g/a_0}}$ reproduces the RAR to $\sim0.1$~dex and, being exponentially close to $3$ at high $g$, leaves Solar System dynamics untouched (corrections $\mathcal{O}(e^{-\sqrt{g/a_0}})\sim e^{-7000}$ at $1$~AU) with no separate screening mechanism.
\subsection{Diversity and Lensing}
Because the transition is set by acceleration, the transition radius $r_M=\sqrt{GM/a_0}$ scales with mass: dwarf galaxies transition at $\sim 1$~kpc, giants at tens of kpc, reproducing the observed diversity of rotation-curve shapes --- a behaviour impossible for any modification acting at a fixed length scale. Crucially, the reduction is a property of the \emph{geometry} through which flux propagates, so it acts identically on the two metric potentials, $\Phi=\Psi$. Gravitational lensing therefore follows the dynamical mass automatically, with a deflection $\hat\alpha = 2\pi v_c^2/c^2$ characteristic of an isothermal profile --- consistent with galaxy--galaxy lensing and the lensing RAR, and \emph{evading by construction} the historical failure of scalar MOND theories in which lensing and dynamics decouple.
\subsection{Epistemic Status}
Equations \eqref{eq:flux}--\eqref{eq:deepmond} are an effective, non-relativistic law: they \emph{describe} and \emph{predict}, and they are falsifiable, but they are not yet \emph{derived} from a covariant action. This is the same status MOND held in 1983. The value of the pillar is that it makes the geometric content explicit (the RAR as a dimensional flow), fixes the diversity and lensing behaviour, and connects to the cosmological sector through $a_0$ (next sections). The covariant realization is the central theoretical open problem, stated as such (Sections~\ref{sec:risks}, \ref{sec:future}).
% ---------------- FIGURE ----------------
\begin{figure}[h!]
\centering
\includegraphics[width=1.0\textwidth]{fig_pillar_flux.pdf}
\caption{\textbf{The pillar} (\texttt{simulations/sim\_pillar\_flux.py}). Rotation curves from the flux law (flat, BTFR-normalized), the RAR derived from $d_{\rm eff}(g)$ with the empirical curve extracted from the data (red), the mass-dependent transition radius $\sqrt{GM/a_0}$ (diversity), and the dimensional flow $d_{\rm eff}$ from $3$ to $2$.}
\label{fig:pillar}
\end{figure}

% ============================================================
\section{Dynamical Consequences: Emergence of MOND}
% ============================================================
From the infrared potential
\begin{equation}
\Phi(r) = v_0^2 \ln\!\left(\frac{r}{r_0}\right),
\label{eq:phi_mond}
\end{equation}
the acceleration is
\begin{equation}
a(r) = -\frac{d\Phi}{dr} = -\frac{v_0^2}{r}.
\label{eq:accel_mond}
\end{equation}
Circular orbits satisfy
\begin{equation}
\frac{v^2}{r} = \frac{v_0^2}{r}
\quad\Rightarrow\quad
v = \text{const}.
\label{eq:vflat}
\end{equation}
The constant velocity satisfies the FSG-\emph{baryonic Tully--Fisher relation}
\begin{equation}
v_0^4 = G M a_0 ,
\label{eq:btfr_v4}
\end{equation}
where $a_0$ is the emergent acceleration scale.
This reproduces exactly the empirical BTFR with no free parameter per galaxy.
% ============================================================
\section{Newtonian vs Fractal Regime}
% ============================================================
In FSG the transition between Newtonian and MOND-like behaviour is controlled
by curvature. Non-locality becomes relevant when
\begin{equation}
R \sim \Lambda.
\label{eq:RsimLambda}
\end{equation}
Thus:
\begin{itemize}
    \item near the mass: $R$ large $\Rightarrow$ GR recovered,
    \item far from the mass: $R$ small $\Rightarrow$ IR fractality, $d_S\to 2$.
\end{itemize}
% --- [V6: A0 FROM MEMORY FIELDS] ---
The acceleration scale is determined by the cosmic expansion history, encoded in the memory fields. Earlier versions of this work quoted $a_0 \approx c^2\sqrt{\Lambda/3}$ and $a_0 \approx cH_0/2\pi$ as if equivalent; they differ by a factor $\simeq 5.7$ and the former is retracted. The correct statement, derived in Section~\ref{sec:memory} from the numerical integration of the memory fields $U = \Box^{-1}R$ and $V = H_0^2\,\Box^{-2}R$ over the cosmic history, is
\begin{equation}
a_{0} = c H_0\, \frac{|V_0|^{3/2}}{|U_0|} \simeq 1.14 \times 10^{-10}\ {\rm m/s^2},
\label{eq:a0_memory}
\end{equation}
within $5\%$ of the observed value $a_0^{\rm obs} \simeq 1.2\times10^{-10}\,{\rm m/s^2}$ with no free parameter; the residual is consistent with the zeroth-order ($\Lambda$CDM background) approximation used in the integration.
This explains:
\begin{itemize}
    \item Newtonian dynamics in the Solar System,
    \item MOND-like dynamics in galaxies,
    \item universality of $a_0$ across galaxies ($1.2 \times 10^{-10} \text{m/s}^2$),
    \item no need for halo-dependent tuning.
\end{itemize}
% ============================================================
\section{Full Derivation of the Fractal Potential}
% ============================================================
The infrared propagator is
\begin{equation}
G(k) \sim \frac{1}{k^3}.
\label{eq:Gk3_repeat}
\end{equation}
In two effective spatial dimensions the potential is
\begin{align}
\Phi(r)
= \int \frac{d^2 k}{(2\pi)^2} \,
\frac{e^{i\vec{k}\cdot\vec{r}}}{k^2}
= v_0^2 \ln r .
\label{eq:potential_2d}
\end{align}
Detailed derivation:
\begin{enumerate}
    \item switch to polar coordinates,
    \item integrate angular direction $\to J_0(kr)$,
    \item use the identity $\displaystyle \int_0^\infty \frac{dk}{k}\,J_0(kr)\propto\ln r$.
\end{enumerate}
The coefficient $v_0$ depends on the baryonic mass:
\begin{equation}
v_0^4 = G M a_0 .
\end{equation}
This is the analytic origin of the BTFR.
% ---------------- FIGURE ----------------
\begin{figure}[h!]
\centering
\includegraphics[width=0.70\textwidth]{fig_newton_vs_fractal.pdf}
\caption{
Newtonian potential ($1/r$) decays rapidly, producing $v\propto r^{-1/2}$.
FSG's fractal potential ($\ln r$) leads to $v=\text{const}$.
}
\label{fig:newton_fractal}
\end{figure}
% ============================================================
\section{Rotation Curves: Analytic Prediction}
% ============================================================

For a baryonic mass distribution $M(r)$, FSG predicts:
\begin{equation}
v^2(r)
= r\frac{d\Phi}{dr}
= v_0^2 .
\label{eq:vFSG_plateau}
\end{equation}

Meanwhile in Newtonian gravity:
\begin{equation}
v^2(r) = \frac{GM(r)}{r},
\label{eq:vNewton}
\end{equation}
which inevitably declines as soon as one exits the baryonic disk.

Thus FSG produces flat rotation curves automatically. We demonstrate this behavior on real observational data (NGC 6503) in Section 14 (see Figure \ref{fig:ngc6503}).

% ============================================================
\section{Baryonic Tully--Fisher Relation (BTFR)}
% ============================================================
One of the most precise empirical laws in extragalactic astronomy is:
\begin{equation}
M_b \propto V_f^4.
\end{equation}
In the deep Infrared (IR) regime, the logarithmic potential (derived in Section 7) implies a constant circular velocity ($v_{\rm const}$), which is the physical origin of the BTFR.
The velocity $v_{\rm const}$ is analytically related to the baryonic mass ($M$) and the cosmic acceleration scale ($a_0$) by:
\begin{equation}
v_{\rm const}^4 = G M a_0 .
\label{eq:btfr_final}
\end{equation}
This formula, which sets both the slope and the normalization, is the \textbf{asymptotic solution} of the FSG field equations in the limit of low acceleration.
% ---------------- FIGURE ----------------
\begin{figure}[h!]
\centering
\includegraphics[width=0.70\textwidth]{fig_btfr.pdf}
\caption{
Baryonic Tully--Fisher relation: observations (SPARC) vs the exact FSG
prediction $M_b \propto V_f^4$ with no free parameters.}
\label{fig:btfr}
\end{figure}
% ============================================================
\section{Radial Acceleration Relation (RAR)}
% ============================================================
Observations show a universal relation
\begin{equation}
g_{\rm obs} = \mathcal{F}(g_{\rm bar}),
\end{equation}
with extremely small intrinsic scatter ($<0.03$ dex).
In FSG:
\[
g_{\rm obs} =
\begin{cases}
g_{\rm bar}, & g_{\rm bar} \gg a_0, \\[0.4em]
\sqrt{g_{\rm bar} a_0}, & g_{\rm bar} \ll a_0.
\end{cases}
\]
This matches exactly the McGaugh--Lelli--Schombert RAR. \textbf{The functional form $\mathcal{F}$ in the transition regime ($\mathbf{g_{\rm bar} \approx a_0}$) is implicitly determined by the full non-linear solutions of the FSG field equations and is a subject of ongoing investigation.}
% ---------------- FIGURE ----------------
\begin{figure}[h!]
\centering
\includegraphics[width=0.70\textwidth]{fig_rar.pdf}
\caption{
Radial Acceleration Relation (RAR): the FSG prediction is identical to the
empirical relation, with transition at $g_{\rm bar}=a_0$.}
\label{fig:rar}
\end{figure}
% ============================================================
\section{Validation on Real Galaxies}
% ============================================================
FSG accurately reproduces the rotation curves of:
\begin{itemize}
    \item NGC 6503 (thin disk, very flat curve),
    \item NGC 2403 (classic MOND-like),
    \item DDO 154 (gas-dominated, MOND ``acid test''),
    \item NGC 5055 (massive spiral),
    \item NGC 2841 (difficult for some MOND variants),
\end{itemize}
with no per-galaxy tuning.
For example NGC 6503: baryonic curves ($v_{\rm bar}(r)$) fall after $r\simeq
4\,{\rm kpc}$, Newtonian prediction declines accordingly, but FSG remains
constant around $116\,{\rm km/s}$, matching the data point-by-point.
% ---------------- FIGURE ----------------
\begin{figure}[h!]
\centering
\includegraphics[width=0.75\textwidth]{fig_ngc6503.pdf}
\caption{
NGC 6503 rotation curve: data vs Newton vs FSG.}
\label{fig:ngc6503}
\end{figure}
% ============================================================
\section{Galaxy Clusters: Memory Modulation and the Bullet Problem}
\label{sec:clusters}
% ============================================================
Galaxy clusters are the classic difficulty of MOND-type theories: in cluster cores the dynamical (hydrostatic or lensing) mass exceeds the deep-MOND prediction from baryons by a factor $2$--$3$, precisely where $g>a_0$ and the pillar alone gives little enhancement. We show that FSG resolves this \emph{mass budget} through a mechanism absent from MOND --- the cosmic memory field --- while being explicit that the distinct problem of the Bullet Cluster \emph{lensing offset} remains open.
\subsection{Memory Modulation of the Acceleration Scale}
In the quasi-static limit the memory field is $U=\Box^{-1}R = 2\Phi/c^2 + U_{\rm cosmo}$ (Section~\ref{sec:memory}): the local potential \emph{is} the local memory layer. Systems differ by the depth of their potential well, $\delta U \equiv 2|\Phi|/c^2$: $\sim10^{-6}$ in galaxies, $\sim10^{-4}$ in cluster cores. We posit that the accumulated memory advances the dimensional transition, raising the effective scale,
\begin{equation}
a_0^{\rm eff} = a_0\left[\,1 + \left(\frac{\delta U}{U_*}\right)^{n}\,\right],
\label{eq:memmod}
\end{equation}
with a single universal $U_*$. In galaxies $\delta U/U_*\ll1$ and \eqref{eq:memmod} is inert (the RAR is untouched); in clusters it boosts the enhancement. This has a precedent in the phenomenological ``EMOND'' proposal (Zhao \& Famaey); here it is native, sourced by the same memory field that generates $a_0$ and dark energy.
\subsection{Test on the Full X-COP Sample}
We confront \eqref{eq:memmod} with the twelve clusters of the XMM Cluster Outskirts Project (X-COP), using the published hydrostatic mass profiles (NFW fiducial), gas-mass and stellar-mass profiles \cite{Eckert2019}. For each cluster we form $g_{\rm obs}=GM_{\rm tot}/r^2$ and $g_{\rm bar}=G(M_{\rm gas}+M_\star)/r^2$, and $\delta U(r)=2|\Phi|/c^2$ from the integrated potential. The pillar prediction with unmodified $a_0$ underestimates $g_{\rm obs}$ by a mean $0.45$~dex (factor $\sim3$ in the cores). A single global fit of \eqref{eq:memmod} gives
\begin{equation}
U_* = 4.5\times10^{-5}, \qquad n = 2.1,
\end{equation}
reducing the residual to $0.10$~dex --- the intrinsic scatter floor of the cluster RAR --- across all twelve clusters (script \texttt{simulations/sim\_xcop\_clusters.py}). Three independent checks support the result: (i) the same $U_*$ leaves the galaxy RAR untouched ($<0.004$~dex shift for $v_{\rm flat}\le300$~km/s); (ii) $U_*$ is stable ($3.8$--$5.4\times10^{-5}$) across mass model (NFW, Einasto), fit window, and outlier removal; (iii) it agrees to $\sim10\%$ with the completely independent value inferred from the CLASH lensing acceleration scale $g^\ddagger=(2.0\pm0.1)\times10^{-9}\,{\rm m/s^2}$ \cite{Tian2020}. The exponent is model-dependent, $n\in[2,3]$; the quadratic form is representative, not proven. The mass-budget problem of clusters is thereby resolved with one universal parameter.
% ---------------- FIGURE ----------------
\begin{figure}[h!]
\centering
\includegraphics[width=1.0\textwidth]{fig_xcop_clusters.pdf}
\caption{\textbf{Cluster mass budget on the 12 X-COP clusters} (\texttt{simulations/sim\_xcop\_clusters.py}). Left: cluster RAR, lying above the galactic relation. Middle: the median deficit $g_{\rm obs}/g_{\rm pred}$ (dot-dashed, factor $\sim3$) closed by memory modulation (solid, unity) with a single $U_*$. Right: the required boost versus memory depth $\delta U$, against $a_0^{\rm eff}=a_0[1+(\delta U/U_*)^2]$.}
\label{fig:xcop}
\end{figure}
\subsection{The Bullet Cluster Offset Remains Open}
The Bullet Cluster (1E~0657-56) poses a \emph{different} problem from the mass budget: the lensing mass peaks are spatially offset from the dominant baryon (the shocked gas), coinciding instead with the subdominant galaxies. We tested whether memory modulation produces this offset, evolving the retarded memory field $\delta U(x,t)$ through a 2D collision for memory timescales $\tau$ from $0.05$ to $10$~Gyr (script \texttt{simulations/sim\_bullet\_memory.py}). \textbf{It does not:} the effective-mass peak remains on the gas for all $\tau$. The reason is structural and we state it plainly --- the effective mass in any potential-sourced modification tracks the dominant baryon (the gas), because the gas dominates the potential at all times, before, during, and after the collision. No memory of a gas-dominated potential can peak on the galaxies.
This is the same wall encountered by every modified-gravity account of the Bullet Cluster; it is not specific to FSG. Earlier FSG versions presented a 1D ``ballistic overshoot'' toy as a candidate mechanism; that toy does not survive a realistic treatment and its suggestion of a solution is withdrawn. The Bullet lensing offset is, honestly, an unsolved problem for FSG, listed as such (Sections~\ref{sec:risks}, \ref{sec:future}). We stress that this does not affect the mass-budget result above, which is a distinct and now quantitatively closed question.

\newpage
% ============================================================
\section{The Strong-Field Regime: Black Holes and the Recovery of General Relativity}
\label{sec:strongfield}
% ============================================================
Having established the infrared (large-scale) behaviour of FSG, we now turn to the opposite extreme --- the strong-field, small-scale regime of black holes. This is the natural test of the claim that FSG is a purely \emph{infrared} modification of gravity: at small scales it must not merely be viable, it must reduce to General Relativity so exactly that no compact-object observation (LIGO/Virgo ringdown, Event Horizon Telescope shadows, pulsar timing) can distinguish it. We show this is automatic, and we quantify the scale at which any deviation could in principle appear.
\subsection{Schwarzschild is an Exact Vacuum Solution of FSG}
Consider the FSG field equations in vacuum ($T_{\mu\nu}=0$). A Schwarzschild black hole is a Ricci-flat solution: $R_{\mu\nu}=0$, hence $R=0$. The non-local field is then
\begin{equation}
X = \Box^{-1}R = \Box^{-1}(0) = X_{\rm cosmo},
\end{equation}
i.e.\ the source term vanishes and $X$ reduces to the homogeneous solution fixed by cosmological boundary conditions --- the same $X_{\rm cosmo}=U_0=\mathcal{O}(10)$ computed in Section~\ref{sec:memory}. On the scale of a black hole ($\sim$km to AU) compared with the Hubble radius ($\sim$Gpc), this background is constant to a precision of order $(r/L)^2 \lesssim 10^{-30}$, so $\nabla X_{\rm cosmo}\to 0$ and
\begin{equation}
f(X) = f(X_{\rm cosmo}) \equiv f_0 = \text{const.}
\end{equation}
With $f_0$ constant and $R=0$, every term of the non-local correction $\Delta G_{\mu\nu}$ that carries a derivative of $f$ or a factor of $R$ vanishes, leaving
\begin{equation}
(1+f_0)\,G_{\mu\nu} = 0 \quad\Longrightarrow\quad G_{\mu\nu}=0.
\end{equation}
The constant $(1+f_0)$ merely renormalizes Newton's constant, $G_{\rm eff}=G/(1+f_0)$, the same rescaling that operates cosmologically. \textbf{The Schwarzschild (and, by the same argument, Kerr) geometry is therefore an exact solution of FSG.} Black-hole shadows, photon rings, innermost stable circular orbits, and quasi-normal ringdown modes are identical to those of GR.
\subsection{The Scale of Any Possible Deviation: the MOND Radius}
The fractional correction becomes dynamically relevant only where the local acceleration falls to the scale $a_0$, i.e.\ at the \emph{MOND radius}
\begin{equation}
r_M = \sqrt{\frac{GM}{a_0}}.
\label{eq:mond_radius}
\end{equation}
Comparing this with the horizon radius $r_s = 2GM/c^2$ gives the scale separation
\begin{equation}
\frac{r_M}{r_s} = \frac{c^2}{2\sqrt{GM\,a_0}},
\end{equation}
which \emph{decreases} with mass but remains astronomically large for every black hole (Table~\ref{tab:bh_scales}). For a stellar-mass black hole the modification is pushed out to $\sim 0.1$~pc, eleven orders of magnitude beyond the horizon; even for the M87* supermassive black hole imaged by the EHT, the separation is a factor $\sim 4\times10^6$. Throughout the entire strong-field region the FSG force law is Newtonian/relativistic, not logarithmic.
\begin{table}[h!]
\centering
\begin{tabular}{lccc}
\hline
Object & Horizon $r_s$ & MOND radius $r_M$ & $r_M/r_s$ \\
\hline
Sun (reference)          & $2.95$~km   & $0.034$~pc & $3.6\times10^{11}$ \\
Stellar BH ($10\,M_\odot$) & $29.5$~km   & $0.11$~pc  & $1.1\times10^{11}$ \\
Sgr A* ($4\times10^6\,M_\odot$) & $0.08$~AU & $68$~pc & $1.8\times10^{8}$ \\
M87* ($6.5\times10^9\,M_\odot$) & $128$~AU & $2.75$~kpc & $4.4\times10^{6}$ \\
\hline
\end{tabular}
\caption{Scale separation between the event horizon and the MOND radius $r_M=\sqrt{GM/a_0}$, computed in \texttt{simulations/sim\_black\_hole.py}. FSG deviates from GR only beyond $r_M$; the strong-field region $r_s<r<r_M$ is pure General Relativity.}
\label{tab:bh_scales}
\end{table}
% ---------------- FIGURE ----------------
\begin{figure}[h!]
\centering
\includegraphics[width=1.0\textwidth]{fig_black_hole_scales.pdf}
\caption{Acceleration law $g(r)$ around a stellar-mass (left) and supermassive (right) black hole. The FSG curve (blue) is indistinguishable from Newton/GR (red dashed) from the horizon $r_s$ outward, departing from it only near the MOND radius $r_M$ where $g$ approaches $a_0$. The strong-field region spans $8$--$11$ decades of pure GR.}
\label{fig:bh_scales}
\end{figure}
\subsection{What FSG Does \emph{Not} Do: the Singularity}
It is important to state a negative result honestly. Because FSG is an infrared modification, it does \emph{nothing} to the central singularity. As $r\to 0$ the curvature diverges, $X$ grows large, and $f(X)\sim X^{-1/2}\to 0$: the theory becomes \emph{more} GR-like, not less. FSG therefore neither softens nor resolves the black-hole singularity; that remains the responsibility of the (unknown) ultraviolet completion discussed in the next subsection and in Section~\ref{sec:risks}. Any claim that the logarithmic potential ``smooths out'' the singularity --- as suggested in an earlier version of our black-hole script --- is incorrect and is retracted.
\newpage
% ============================================================
\section{Non-Local Formalism: \texorpdfstring{$R\Box^{-1}R$}{R Box^{-1} R},
\texorpdfstring{$R\Box^{-2}R$}{R Box^{-2} R}, and Fractal Geometry}
% ============================================================
The most successful non-local cosmological models include:
\begin{itemize}
    \item the RR model ($R\,\Box^{-2}R$) \cite{Maggiore2014},
    \item the RT model ($R\,\Box^{-1}g_{\mu\nu}T^{\mu\nu}$) \cite{Dirian2014},
    \item the original non-local model of Deser--Woodard \cite{Deser2007}.
\end{itemize}
These models share key properties:
\begin{enumerate}
    \item They reproduce CMB/BAO/SN\,Ia constraints.
    \item They generate dynamical dark energy without a cosmological constant.
    \item Their equation of state crosses $w = -1$ naturally.
\end{enumerate}
FSG belongs to this family, but adds a qualitatively new ingredient:
\begin{center}
\textbf{IR dimensional reduction:} $d_S\to 2$ as $k\to 0$.
\end{center}
This explains:
\begin{itemize}
    \item the acceleration scale $a_0$,
    \item BTFR,
    \item early structure formation,
    \item dynamical dark energy.
\end{itemize}
% ============================================================
\section{Geometric Memory: \texorpdfstring{$U=\Box^{-1}R$}{U=Box^{-1}R},
\texorpdfstring{$V=\Box^{-2}R$}{V=Box^{-2}R}}
\label{sec:memory}
% ============================================================
Following standard non-local techniques, we introduce the auxiliary fields:
\begin{equation}
U = \Box^{-1}R, \qquad
V = H_0^2\,\Box^{-1}U,
\end{equation}
where the factor $H_0^2$ renders $V$ dimensionless. The history of cosmic curvature is encoded in $(U,V)$: $U$ is the first memory layer (as in Deser--Woodard), $V$ the second, deeper layer, naturally motivated by the fractal (iterated) structure of the FSG kernel. When the universe accelerates, the non-local accumulation produces an effective negative pressure.
\subsection{Numerical Integration over Cosmic History}
On an FLRW background, with $x = \ln a$ and $h = H/H_0$, the fields obey
\begin{align}
U'' + \Bigl(3 + \frac{h'}{h}\Bigr)U' &= -6\Bigl(\frac{h'}{h} + 2\Bigr), \\
V'' + \Bigl(3 + \frac{h'}{h}\Bigr)V' &= -\frac{U}{h^2},
\end{align}
with $U = V = 0$ deep in the radiation era (where $R = 0$). Integrating on a Planck-2018 $\Lambda$CDM background (zeroth order in back-reaction; script \texttt{simulations/sim\_friedmann\_fsg.py} in the code repository) yields
\begin{equation}
U_0 \equiv U(a{=}1) = -16.1, \qquad V_0 \equiv V(a{=}1) = +1.99,
\end{equation}
consistent in magnitude with the classic Deser--Woodard value $X_{\rm today} \approx -14$ \cite{Deser2007}.
\subsection{The Acceleration Scale from Cosmic Memory}
The MOND acceleration scale emerges from the memory fields as
\begin{equation}
a_0 = c H_0\,\frac{|V_0|^{3/2}}{|U_0|}
 = 1.14\times 10^{-10}\ {\rm m/s^2},
\label{eq:a0_derived}
\end{equation}
within $4.8\%$ of the observed $a_0 \simeq 1.2\times10^{-10}\,{\rm m/s^2}$, with no free parameter. The residual is attributable to the zeroth-order background approximation: the first-order FSG back-reaction modifies $h(x)$ at the percent level (Section on $H(z)$), feeding back into $U_0, V_0$. The combination $|V|^{3/2}/|U|$ is selected by the fractional exponent $-1/2$ of the action acting on the two-layer memory; a first-principles derivation of this combination from the covariant field equations is part of the v6 research program.
% ============================
\subsection{Solar System Tests Without Screening: the Perturbative PPN Calculation}
% ============================
Earlier versions of this work computed $|\gamma_{\rm PPN}-1|$ by treating FSG as a scalar--tensor theory with an effective Brans--Dicke parameter, found a catastrophic deviation, and invoked a screening mechanism plus UV regularization to suppress it. \emph{That treatment was a category error, and both the Brans--Dicke estimate and the screening mechanism are retracted.} $X = \Box^{-1}R$ is not an independent propagating scalar with its own kinetic term: it is a \emph{functional of the metric}, and the correct procedure is a perturbative evaluation of the non-local terms around the cosmological background, exactly as in the RR model of Maggiore--Mancarella \cite{Maggiore2014} and its confrontation with data \cite{Dirian2014}, where Solar System consistency is automatic.
Inside the Solar System, the field $X$ is dominated by its cosmological background value $\bar{X} = U_0 = \mathcal{O}(10)$ (Section~\ref{sec:memory}); the local source perturbs it only by $\delta X \sim \Phi/c^2$. Expanding $f(X) = \gamma(L^2/X)^{1/2}$ around $\bar{X}$:
\begin{equation}
\delta\gamma_{\rm PPN} \sim \frac{f'(\bar{X})}{1+f(\bar{X})}\,\delta X \cdot \frac{\Phi}{c^2}
\;\sim\; \mathcal{O}(1)\times\left(\frac{\Phi}{c^2}\right)^{2}.
\end{equation}
For the Sun at $r = 1\,{\rm AU}$, $\Phi/c^2 \simeq 10^{-8}$, hence
\begin{equation}
|\gamma_{\rm PPN} - 1| \;\sim\; 10^{-16} \text{--} 10^{-20},
\end{equation}
many orders of magnitude below the Cassini bound $|\gamma - 1| < 2.3\times10^{-5}$. The physical reason is structural: the non-local modification is sourced by the \emph{cosmological} curvature history, which is homogeneous across the Solar System; local dynamics only probes its gradients, which are second order in $\Phi/c^2$. FSG is therefore naturally compatible with Solar System tests, \emph{without any screening mechanism}, for the same reason as the RR and RT non-local models \cite{Maggiore2014,Dirian2014}. The parameter $\beta$ follows the same pattern, $\beta \to 1$. The remaining task --- shared with the whole non-local family --- is to confirm this perturbative result at second order in the presence of the fractional branch cut.
% ============================================================
\section{Predicted Equation of State: Dynamic \texorpdfstring{$w(z)$}{w(z)}}
% ============================================================
Unlike $\Lambda$CDM where $w=-1$ is constant, FSG predicts a dynamical evolution driven by the growth of the auxiliary fields $U$ and $V$ (Section~\ref{sec:memory}).
\subsection{The Structural Argument for Phantom Behaviour}
The robust, scheme-independent statement is the following. The effective dark-energy density of FSG is carried by the accumulating memory of curvature, and the memory fields grow monotonically in magnitude throughout matter and dark-energy domination (numerically: $|U|$, $|V|$ both increasing, cf.\ Section~\ref{sec:memory}). By the continuity equation, any dark-energy density that \emph{grows} with cosmic time has
\begin{equation}
w(z) = -1 - \frac{1}{3}\,\frac{d\ln\rho_{\rm DE}}{d\ln a} \;<\; -1 .
\end{equation}
Phantom behaviour ($w_0 < -1$) is therefore a structural consequence of growing memory, independent of the detailed localization scheme.
\subsection{Quantitative Bracket at First Order}
The magnitude of $|w_0 + 1|$ does depend on which combination of the memory fields carries $\rho_{\rm DE}$, which requires the full covariant back-reaction (future work). Two calculable anchors bracket the prediction:
\begin{itemize}
\item \textbf{Lower bound (benchmark):} the RR model of Maggiore--Mancarella, the closest fully-solved member of the same operator family, gives $w_0 \simeq -1.14$ \cite{Maggiore2014,Dirian2014}.
\item \textbf{Upper bound (first-order estimate):} assuming $\rho_{\rm DE}$ tracks the deep memory layer $|V(a)|$ (normalized to $\Omega_\Lambda$ today; script \texttt{simulations/sim\_friedmann\_fsg.py}) gives $w_0 \simeq -1.7$. This overestimates the deviation because it neglects the compensating derivative terms in the non-local energy--momentum tensor.
\end{itemize}
The honest v6 prediction is thus $-1.7 \lesssim w_0 \lesssim -1.1$, with the structural inequality $w_0 < -1$ as the falsifiable core. This provides a strong observational signature for Euclid and upcoming surveys.
% ---------------- FIGURE ----------------
\begin{figure}[h!]
\centering
\includegraphics[width=0.75\textwidth]{fig_wz.pdf}
\caption{
Equation of state $w(z)$ for $\Lambda$CDM ($w=-1$), FSG-RT (mild non-locality),
and FSG-RR (strong non-locality). Euclid can resolve differences at the
percent level.}
\label{fig:wz}
\end{figure}
% ============================================================
\section{Expansion History: Deviation in \texorpdfstring{$H(z)$}{H(z)}}
% ============================================================
Define:
\[
E(z) = \frac{H(z)}{H_0}.
\]
Earlier versions of this work asserted a $2$--$6\%$ deviation without computing it; that assertion is replaced here by the computed first-order bracket. Recomputing $H(z)$ with the memory-driven dark-energy density of the previous section (normalized to the same $\Omega_m$ and $H_0$ today; script \texttt{simulations/sim\_friedmann\_fsg.py}) gives
\begin{equation}
\left|\frac{H_{\rm FSG}(z)-H_{\Lambda{\rm CDM}}(z)}
     {H_{\Lambda{\rm CDM}}(z)}\right|
\;\in\; [\,1\%,\ 13\%\,] \quad \text{for } 0.5<z<2,
\label{eq:Hz_deviation}
\end{equation}
where the lower end corresponds to the RR-model benchmark ($\Delta H/H \simeq 1$--$2\%$ \cite{Dirian2014}) and the upper end to the first-order $V$-tracking estimate (an upper bound, for the reason given above); the deviation vanishes at $z=0$ by normalization and tends to zero for $z>3$. Even the lower end of the bracket exceeds Euclid's expected accuracy ($<0.5\%$), so the prediction is testable across its entire range.
% ---------------- FIGURE ----------------
\begin{figure}[h!]
\centering
\includegraphics[width=0.72\textwidth]{fig_Hz.pdf}
\caption{
Relative deviation of $H(z)$ between FSG and $\Lambda$CDM. Euclid will detect
differences $>1\%$.}
\label{fig:Hz}
\end{figure}
% ============================================================
\section{Falsifiability of FSG}
% ============================================================
FSG is genuinely falsifiable. The theory requires:
\begin{equation}
w_0 < -1.
\end{equation}
Thus:
\begin{itemize}
    \item If Euclid measures $w_0 = -1.00 \pm 0.01$, FSG is ruled out.
    \item If $w_0 < -1$, then $\Lambda$CDM is ruled out.
\end{itemize}
The prediction is not optional: it is a structural consequence of the
non-local IR deformation.
% ============================================================
\section{Structure Formation: JWST Predictions}
% ============================================================
The linear growth equation is
\begin{equation}
\delta'' + A(a)\delta' - B(a)\delta = 0.
\end{equation}
In FSG the effective Newton’s constant becomes
\[
G_{\rm eff} = G\,(1+\epsilon),
\qquad 0.1<\epsilon<0.25.
\]
Thus:
\begin{itemize}
    \item $\Lambda$CDM first massive galaxies: $z\approx 4$,
    \item FSG first massive galaxies: $z\approx 15$--20.
\end{itemize}
This matches JWST observations of massive galaxies at $z>10$.
% ---------------- FIGURE ----------------
\begin{figure}[h!]
\centering
\includegraphics[width=1.0\textwidth]{fig_fsg_simulation_results.pdf}
\caption{\textbf{Numerical Validation of FSG.} 
\textbf{(Left)} Galaxy rotation curves computed via 3D FFT convolution of the FSG propagator on a $64^3$ grid. The blue curve naturally flattens due to the IR dimensional reduction ($d_S \to 2$), reproducing observations without Dark Matter.
\textbf{(Right)} Solution of the linear growth equation for cosmic structures. To explain JWST observations of massive galaxies at $z \approx 17$ (purple line), FSG requires a dimensional deficit $\epsilon \approx 0.09$, whereas standard $\Lambda$CDM (red dashed) predicts formation much later at $z \approx 4$.}
\label{fig:fsg_results}
\end{figure}
% ============================
\section{The CMB: What Is Established, What Is Conjectured}
% ============================
A major challenge for modified gravity theories is to reproduce the angular power spectrum of the Cosmic Microwave Background (CMB), specifically the height of the third acoustic peak. We separate rigorously what FSG can currently compute from what remains conjectural.
\subsection{Established: Peak Positions (Geometry)}
The angular position of the acoustic peaks depends only on the background geometry, through the ratio of the comoving sound horizon at decoupling $r_s(z_{\rm dec})$ to the comoving distance $\chi(z_{\rm dec})$. Since the FSG modification is infrared/late-time, the pre-recombination physics is unchanged and $r_s$ is identical to $\Lambda$CDM ($r_s = 144.2\,$Mpc with Planck-2018 parameters). Computing $\chi(z_{\rm dec})$ with a phantom equation of state $w_0 = -1.1$ (conservative end of the FSG bracket; script \texttt{simulations/sim\_cmb\_peaks.py}) shifts the peak positions by
\begin{equation}
\frac{\Delta \ell_n}{\ell_n} \simeq +0.8\% \quad \text{at fixed } H_0.
\end{equation}
This shift is absorbed by the well-known geometric degeneracy of the CMB between $w$ and $H_0$: at fixed acoustic angle $\theta_\star$ (measured by Planck to $0.03\%$), a phantom $w_0$ is compensated by a \emph{higher} $H_0$ --- a direction favourable to the Hubble tension. The CMB peak geometry therefore does not exclude FSG.
\subsection{Estimated: the Late-Time ISW Effect}
The modified growth of potentials at $z \lesssim 2$ enhances the late Integrated Sachs--Wolfe contribution at low multipoles ($\ell \lesssim 30$). Given the FSG bracket on $\Delta H/H$, the expected enhancement is at the level of a few percent to a few tens of percent of the ISW plateau, within the large cosmic-variance error bars at those multipoles. A definite number requires the covariant perturbation equations.
\subsection{Conjectured: Peak Amplitudes (the ``Fractal Boost'')}
Without Cold Dark Matter, sustaining the observed relative heights of the acoustic peaks requires a scale-dependent enhancement of the effective gravitational coupling $G_{\rm eff}(k)$ around recombination --- the ``Fractal Boost''. \textbf{This remains a conjecture.} We state explicitly what previous versions of this paper blurred: the numerical integration presented in Appendix~\ref{app:cmb_check} solves a \emph{modified Jeans equation} for the baryon fluid. It contains no photon Boltzmann hierarchy, no Thomson coupling, no metric potentials $\Phi, \Psi$, and no neutrinos; the oscillations it displays are \emph{Jeans oscillations}, not CMB acoustic peaks, and that appendix is retained only as a toy illustration of the boost mechanism. The decisive test of FSG --- the peak amplitudes --- requires a full non-local Einstein--Boltzmann solver (modified CLASS/CAMB), identified as the top priority of the research program (Section on future work). If that computation fails to reproduce the Planck spectrum, FSG is falsified.
% NOTE: The conceptual figure has been removed to prioritize the numerical results in Appendix A.
% ============================================================
\section{Spectral Dimension as an Observable}
% ============================================================
The running of the spectral dimension is defined as:
\begin{equation}
d_S(k) = 2 + \frac{\partial\ln G^{-1}(k)}{\partial\ln k}.
\end{equation}
FSG predicts:
\begin{align}
d_S &\approx 3  && (\text{galactic scales}),\\
d_S &\approx 2.9 && (\text{Mpc scales}),\\
d_S &\to 2       && (\text{cosmological IR}).
\end{align}
Weak lensing surveys may detect this weakening of the effective dimensionality.
% ============================================================
\section{Synthesis Diagram: The Three Regimes}
% ============================================================
\begin{figure}[h!]
\centering
\includegraphics[width=0.92\textwidth]{fig_three_scales.pdf}
\caption{
Schematic summary of the three regimes of FSG:
(1) Newton/GR regime ($d_S=4$),
(2) transition regime ($d_S\approx 3$),
(3) fractal IR regime ($d_S\to 2$).}
\label{fig:three_scales}
\end{figure}
% ============================================================
\section{Cosmological Predictions Summary}
% ============================================================
\begin{table}[h!]
\centering
\begin{tabular}{lccc}
\hline
Observable & $\Lambda$CDM & FSG (RR/RT) & Favored \\
\hline
$w(z)$ & $-1$ fixed & $w_0<-1$ & FSG \\
Phantom crossing & impossible & mandatory & FSG \\
$H(z)$ & fixed & $1$--$13\%$ (bracket) & Euclid-testable \\
Structure formation & $z\sim4$ & $z\sim15$--20 & FSG\,+\,JWST \\
Tully--Fisher & emergent & exact & FSG \\
RAR & noisy & exact & FSG \\
$a_0$ & fit & derived & FSG \\
\hline
\end{tabular}
\caption{
Summary of key predictions of FSG versus $\Lambda$CDM.}
\label{table:summary}
\end{table}
\newpage
% ============================================================
\section{Progress Toward a Covariant Completion: the Aether--Scalar--Tensor Correspondence}
\label{sec:aest}
% ============================================================
The central open problem of FSG is the covariant action realizing the pillar (Sections~\ref{sec:risks}, \ref{sec:future}). This section records concrete progress toward it made since the first v6 release. The results below are \emph{structural correspondences}, not a completed derivation; the honest boundaries are collected at the end (Section~\ref{sec:aest_open}).

\subsection{Why the Two No-Go Results Occurred}
Both retracted attempts coupled the memory as a \emph{source}: a curvature-memory term $R\,f(\Box^{-1}R)$, or a kinetic-memory term $(\partial\,\Box^{-1}R)^2$ added to the action. The first makes the modification potential-dependent, forcing Keplerian rather than flat curves; the second introduces a second independent metric potential and hence gravitational slip $\Phi\neq\Psi$, spoiling lensing. The common flaw is now identifiable: the pillar is not the \emph{addition of a field}, but a modification of how the \emph{single} gravitational potential responds to its source --- the flux propagating through $d_{\rm eff}$ dimensions. Cast this way, the modification belongs to the Bekenstein--Milgrom / QUMOND class (a single potential with a modified kinetic term), whose relativistic completion is the aether / preferred-foliation class --- the same anisotropic structure suggested independently by the $d_{\rm eff}\to2$ behaviour common to the deep infrared and the ultraviolet.

\subsection{The Single-Potential Form Passes Both Tests}
Written as a QUMOND-type law $g=\nu(g_N/a_0)\,g_N$ with $\nu(y)=[1-e^{-\sqrt{y}}]^{-1}$ derived from $d_{\rm eff}=3-e^{-\sqrt{g_N/a_0}}$, FSG is a single-potential theory. Numerically (\texttt{simulations/sim\_covariant\_aest.py}) it (i) yields flat rotation curves and the exact BTFR --- evading no-go~1 --- and (ii) has $\Phi=\Psi$ identically, so the lensing mass equals the dynamical mass --- evading no-go~2. The two walls that stopped the scalar-addition attempts are both consequences of introducing a \emph{second} potential; the single-potential flux formulation removes them by construction.

\subsection{The Free Function Fixed by $d_{\rm eff}$}
In AQUAL form, $\mathcal{L}=-(a_0^2/8\pi G)\,F(X)$ with $X=|\nabla\Phi|^2/a_0^2$ and interpolation $\mu(x)=F'(x^2)$. Reconstructing $F$ from FSG's $d_{\rm eff}$-derived $\mu$ (\texttt{simulations/sim\_aest\_freefunction.py}) gives a function interpolating between the deep-MOND form $F\to\tfrac{2}{3}X^{3/2}$ (verified to $0.2\%$) and the Newtonian form $F\to X$ ($F'\to1$). These are precisely the two limits required of the free function in the relativistic Aether--Scalar--Tensor (AeST) theory of Skordis and Z\l osnik \cite{SkordisZlosnik2021}, which is established to reproduce MOND rotation curves, $\Phi=\Psi$ lensing, \emph{and} the CMB acoustic-peak spectrum. FSG's flux dimension therefore \emph{fixes} an AeST-class free function, the exponential interpolation in between being the geometric fingerprint of $d_{\rm eff}$.

\subsection{The Cosmological Sector from the Memory Field}
AeST reproduces the CMB because its scalar carries an energy density scaling dust-like ($\propto a^{-3}$) during matter domination. FSG already possesses a cosmological scalar sector: the memory field $U=\Box^{-1}R$. Integrating $U$ on the FLRW background (\texttt{simulations/sim\_memory\_cosmo\_scaling.py}) gives $U(a{=}1)=-16.1$ --- the \emph{same} value that fixes $a_0$ in Section~\ref{sec:memory} --- with the steady state $U'\to-2$ in matter domination, so that the natural kinetic memory density $\rho_U\sim(\dot U)^2\propto a^{-3}$ (dust-like) through the CMB epoch and $\to\,$const at late times (dark-energy-like). A single memory field thus carries the scaling of \emph{both} dark sectors at the epochs where each is required, driven by the same $U_0$ that already sets the acceleration scale.

\subsection{Honest Boundaries}
\label{sec:aest_open}
These are necessary correspondences, not sufficiency proofs, and three boundaries must be stated plainly.
\begin{enumerate}
\item FSG's $U=\Box^{-1}R$ is \emph{slaved} to the curvature --- a more constrained object than AeST's freely-specifiable scalar. It is not established that this restriction inherits the full AeST CMB success.
\item Matching the free-function limits and the density \emph{scaling} does not fix the \emph{amplitudes} (the $\Omega$-equivalent and the peak heights), which require the perturbed non-local calculation of Priority~4 (Section~\ref{sec:future}).
\item At the quasi-static level the memory potential of an isolated void \emph{grows} ($\propto\sqrt{D}$) rather than decaying, giving the \emph{wrong} sign for the CMB Cold Spot integrated Sachs--Wolfe imprint; whether the full retarded memory dynamics restores the observed cold sign is open and tied to the same perturbed calculation.
\end{enumerate}
The value of the correspondence is that it converts the covariant-action problem from ``no known route'' into ``a mapping onto a known, viable class'', and it explains why the earlier scalar-addition attempts failed. It does not, by itself, promote FSG from an effective law to a validated fundamental theory.
% ============================================================
\section{Theoretical Risks: How FSG Could Be Refuted}
\label{sec:risks}
% ============================================================
A scientifically healthy theory must be vulnerable to data.  
FSG can be falsified by:
\subsection{Euclid measures \texorpdfstring{$w_0=-1$}{w0=-1}}
If
\[
w_0 = -1.00 \pm 0.01,
\]
FSG is ruled out; $\Lambda$CDM is confirmed.
\subsection{Detection of Cold Dark Matter Particles}
If experiments detect WIMPs, axions, or sterile neutrinos with correct relic
density, there is no longer motivation to remove dark matter.
\subsection{The Bullet Cluster Lensing Offset (Open)}
The cluster \emph{mass budget} is resolved by memory modulation (Section~\ref{sec:clusters}), but the \emph{spatial lensing offset} of the Bullet Cluster is not: our 2D time-dependent test shows the effective-mass peak stays on the gas for all memory timescales, because any potential-sourced modification tracks the dominant baryon. This is an unsolved problem, shared with all modified-gravity approaches. Should it be shown that \emph{no} FSG mechanism can place the lensing peak on the galaxies without invoking additional (e.g.\ cold-baryonic or sterile-neutrino) mass, this remains a permanent difficulty for the framework on colliding systems.
\subsection{Absence of a Covariant Action for the Pillar (Central Open Problem)}
The galactic sector rests on the pillar (Section~\ref{sec:pillar}) as an \emph{effective} flux-reduction law. Two attempts to derive it from a covariant action have failed by explicit no-go results and are retracted. Section~\ref{sec:aest} records progress: the failures are traced to coupling the memory as a \emph{source} rather than as a modification of the single potential, and the single-potential (QUMOND) formulation maps onto the AeST class \cite{SkordisZlosnik2021} with a free function fixed by $d_{\rm eff}(g)$. This is a candidate route, not a completed action. If it can nonetheless be proven that \emph{no} covariant, ghost-free action reduces to the acceleration-dependent dimensional flow $d_{\rm eff}(g)$ --- as opposed to a potential-dependent or fixed-length modification --- then FSG cannot be promoted from an effective law to a fundamental theory, and stands only at the phenomenological level of MOND.
\subsection{The CMB "Acid Test" (Boltzmann Code)}
The most critical challenge for FSG is to reproduce the Cosmic Microwave Background (CMB) angular power spectrum, particularly the third acoustic peak, without Cold Dark Matter.
We propose a "Fractal Boost" mechanism where the effective gravitational coupling $G_{\rm eff}(k)$ increases in the infrared, potentially deepening potential wells during recombination.
\textbf{However, this remains a conjecture.}
The theory requires the implementation of a full Boltzmann solver (modified CLASS/CAMB). If the numerical integration of the non-local equations fails to match the Planck data—specifically the relative heights and phases of the acoustic peaks—\textbf{FSG is falsified}.
\subsection{Failure of the Perturbative PPN Result at Higher Order}
As of v6 the theory no longer relies on any screening mechanism: the perturbative calculation of Section~\ref{sec:memory} gives $|\gamma_{\rm PPN}-1| \sim 10^{-16}$--$10^{-20}$, far below all Solar System bounds, because the non-local sector is sourced by the homogeneous cosmological background. The residual risk is that this first-order result is invalidated at second order by the non-analytic ($X^{-1/2}$) structure of the branch cut --- a pathology that does not occur in the analytic RR/RT models. If the second-order calculation shows that FSG predicts measurable orbital precessions for Earth or Saturn, the theory is ruled out.
\subsection{UV Completion and Vacuum Stability: Status and Open Problems}
\label{sec:uv}
A fractional operator of the form $X^{-1/2}$ raises two distinct ultraviolet concerns, which earlier versions of this paper conflated and over-claimed. We separate them.
\paragraph{(a) The infinite-derivative form factor (tractable).}
We regularize the action with an \emph{entire} (infinite-derivative) form factor,
\begin{equation}
S_{UV} = \frac{M_P^2}{2} \int d^4x \sqrt{-g} \, R \, \left[ 1 + \left(\frac{L^2}{\Box^{-1}R}\right)^{\frac{1}{2}}\, e^{-\Box / M_*^2} \right],
\label{eq:regularized_action}
\end{equation}
with $M_* \gg 1$~TeV a UV scale. Around flat space the graviton propagator acquires a factor $e^{-k^2/M_*^2}$. Because an entire function $e^{H(\Box)}$ with $H$ entire has \emph{no zeros}, it introduces \emph{no new poles} in the complex $k^2$ plane: the propagator retains only the massless $k^2=0$ pole, i.e.\ the transverse-traceless spin-2 graviton, with no Ostrogradsky ghost. This is the standard result of infinite-derivative (non-local) ghost-free gravity established by Biswas, Gerwick, Koivisto and Mazumdar \cite{Biswas2012} and by Modesto and Tomboulis \cite{Modesto2012,Tomboulis1997}; it holds \emph{at tree level}. In the UV ($k^2 \gg M_*^2$) the correction is exponentially suppressed and the action reduces to Einstein--Hilbert, recovering exact General Relativity --- consistent with the strong-field results of Section~\ref{sec:strongfield}.
\paragraph{(b) The fractional branch cut (open).}
The suppression in (a) controls the UV but does not remove the non-analytic $X^{-1/2}$ branch-cut structure, which lives in the \emph{infrared} and is precisely the feature responsible for MOND dynamics --- it is a feature, not a UV pathology. Its status is nonetheless not fully settled: a complete demonstration of unitarity requires (i) a loop-level analysis, which for infinite-derivative gravity is subtle and still debated even in the analytic case, and (ii) a non-perturbative treatment of the branch cut, absent in the analytic RR/RT models. We therefore state the honest position: FSG is \emph{tree-level ghost-free} under the entire-form-factor completion~\eqref{eq:regularized_action}, and its full quantum consistency is an open problem, not a proven theorem. (The previous invocation of the Vafa--Witten theorem was a misattribution --- that result concerns parity and vector-symmetry realization in vector-like gauge theories, not unitarity of non-local gravity --- and is withdrawn.)
\paragraph{Falsifiable content.} If a rigorous analysis shows that no entire form factor can simultaneously (i) preserve the IR $X^{-1/2}$ branch cut required for MOND and (ii) maintain unitarity, then the FSG action has no consistent UV completion and the framework fails at the level of principle.
% ============================================================
\section{Strengths of FSG}
% ============================================================
\begin{itemize}
    \item One single scale: $a_0 = cH_0\,|V_0|^{3/2}/|U_0|$, derived from the cosmic memory fields (not fitted), within $5\%$ of observation.
    \item The pillar (flux $3\to2$) yields the \emph{exact} BTFR and makes the RAR a measurement of the dimensional flow; lensing follows dynamics by construction ($\Phi=\Psi$).
    \item Galaxy clusters: the MOND mass deficit closed on the full X-COP sample with one memory-modulation parameter, cross-validated at $10\%$ by CLASH lensing.
    \item Exact recovery of GR in the strong field: Schwarzschild/Kerr up to $\mathcal{O}(e^{-g/a_0})$.
    \item Rotation-curve diversity from the mass-dependent transition radius $\sqrt{GM/a_0}$.
    \item JWST predictions matched (early massive galaxies).
    \item Precise, quantitative, falsifiable predictions for $w(z)$, $H(z)$,
          BTFR, RAR, and spectral dimension.
    \item Deep conceptual relation between galactic dynamics and cosmic horizon.
\end{itemize}
% ============================================================
\section{Perspectives for Future Development}
\label{sec:future}
% ============================================================
\subsection{A Covariant Action for the Pillar (Priority 1)}
The central task is to find a covariant, ghost-free action whose quasi-static limit reproduces the flux law $d_{\rm eff}(g)$ of Section~\ref{sec:pillar} --- an \emph{acceleration}-dependent dimensional flow. The two natural candidates within the non-local family (curvature memory $R\,f(\Box^{-1}R)$ and kinetic memory $(\partial\Box^{-1}R)^2$) are excluded by the no-go results of this paper. The correspondence of Section~\ref{sec:aest} identifies the most concrete route: the single-potential formulation places FSG in the Aether--Scalar--Tensor (AeST) class \cite{SkordisZlosnik2021}, whose free function is reproduced by $d_{\rm eff}(g)$ (with the correct $\tfrac23 X^{3/2}$ and $X$ limits) and whose dark sector has the same $a^{-3}$/const scaling carried by the FSG memory field $U=\Box^{-1}R$. The remaining task is to determine whether the \emph{slaved} choice $U=\Box^{-1}R$ --- more constrained than AeST's free scalar --- reproduces the AeST CMB spectrum, i.e.\ to complete the mapping at the level of amplitudes rather than scalings. This is the same computation required by Priority~4.
\subsection{Covariant Back-Reaction (Priority 2)}
The memory-field values $(U_0, V_0)$ entering $a_0$, and the bracket on $w(z)$ and $H(z)$, are computed at zeroth/first order on a $\Lambda$CDM background. The full covariant integration of the FSG Friedmann equations is required to close the $5\%$ residual on $a_0$, collapse the $[-1.7,-1.1]$ bracket on $w_0$, and derive from first principles the combination $|V|^{3/2}/|U|$ and the modulation parameters $(U_*,n)$ of the cluster sector.
\subsection{The Bullet Cluster and Colliding Systems (Priority 3)}
A mechanism placing the lensing peak on the collisionless component, without collisionless matter, remains to be found (Section~\ref{sec:clusters}).
\subsection{Boltzmann Code Implementation (Priority 4)}
A non-local Einstein-Boltzmann solver is required. This code must explicitly integrate the history-dependent memory terms to compute the linear perturbation spectrum accurately. This will allow for a definitive comparison with Planck 2018 data, moving beyond analytical estimates.
\subsection{Towards a Fundamental Derivation: The Bottom-Up Approach}
Critics may argue that the fractional action $S \sim R[1+(L^2/X)^{1/2}]$ is reverse-engineered (ad hoc) to fit galactic dynamics. We acknowledge this phenomenological origin. However, in the spirit of the \textbf{Ginzburg-Landau theory} of superconductivity, FSG should be understood as a "Bottom-Up" reconstruction of the effective field theory of gravity in the infrared.
Just as Ginzburg-Landau identified the order parameter before the microscopic BCS theory was developed, FSG identifies the \textbf{fractional scaling} required to satisfy observations. The explicit form of the operator likely emerges from the \textbf{coarse-graining} of a fundamental quantum geometry (such as Causal Dynamical Triangulations or Spin Foams) where the spectral dimension flows from $d_S=4$ to $d_S=2$.
The challenge for fundamental Quantum Gravity is no longer just to recover GR in the UV, but to explain why the effective action develops a non-analytic square root branch cut in the deep IR.
% ============================================================
\section{Conclusion}
% ============================================================
Fractal Spacetime Gravity (FSG) provides a coherent, predictive framework
linking:
\begin{itemize}
    \item galactic dynamics (BTFR, RAR, flat curves),
    \item cosmology (accelerated expansion without $\Lambda$),
    \item early structure formation (JWST),
    \item IR dimensional reduction ($d_S\to2$),
    \item non-local effective field theory ($R\Box^{-1}R$).
\end{itemize}
FSG is not a flexible model:  
its predictions are rigid and falsifiable.  
Upcoming missions (Euclid, Roman, SKA, CMB-S4, JWST) will determine whether:
\begin{center}
\textbf{FSG becomes a viable alternative to $\Lambda$CDM,}\\[0.3em]
\textbf{or is refuted --- as a scientific theory should be.}
\end{center}
\newpage
% ============================
\section*{Acknowledgements and Call for Collaboration}
% ============================
The author acknowledges that the full validation of the CMB predictions requires a dedicated non-local Boltzmann code, a task of significant complexity. 
Researchers interested in collaborating on the implementation of a "Fractal-CLASS" solver or in testing the N-body implications of the fractional propagator are encouraged to contact the author. 
Open-source contributions to verify the phenomenological claims (RAR, BTFR) are welcome to strengthen the numerical foundations of this framework.
% ============================================================
\begin{thebibliography}{99}
% ============================================================
% MOND & galactic dynamics
\bibitem{Milgrom1983}
M.~Milgrom,
\emph{A modification of the Newtonian dynamics as a possible alternative to the hidden mass hypothesis},
Astrophys.\ J.\ \textbf{270}, 365 (1983).
\bibitem{Milgrom2014}
M.~Milgrom,
\emph{The MOND paradigm},
Scholarpedia \textbf{9}, 31410 (2014).
\bibitem{McGaughRAR2016}
S.~McGaugh, F.~Lelli, J.~Schombert,
\emph{The Radial Acceleration Relation in Rotationally Supported Galaxies},
Phys.\ Rev.\ Lett.\ \textbf{117}, 201101 (2016).
\bibitem{SPARC2016}
F.~Lelli, S.~McGaugh, J.~Schombert,
\emph{SPARC: Mass Models for 175 Disk Galaxies},
AJ \textbf{152}, 157 (2016).
\bibitem{McGaughBTFR2005}
S.~McGaugh,
\emph{The Baryonic Tully–Fisher Relation of Galaxies with Extended Rotation Curves},
Astrophys.\ J.\ \textbf{632}, 859 (2005).
% Non-local gravity
\bibitem{Deser2007}
S.~Deser, R.~P.~Woodard,
\emph{Nonlocal Cosmology},
Phys.\ Rev.\ Lett.\ \textbf{99}, 111301 (2007).
\bibitem{SkordisZlosnik2021}
C.~Skordis, T.~Z\l osnik,
\emph{New Relativistic Theory for Modified Newtonian Dynamics},
Phys.\ Rev.\ Lett.\ \textbf{127}, 161302 (2021).
\bibitem{Maggiore2014}
M.~Maggiore, M.~Mancarella,
\emph{Nonlocal gravity and dark energy},
Phys.\ Rev.\ D \textbf{90}, 023005 (2014).
\bibitem{Dirian2014}
Y.~Dirian et al.,
\emph{Nonlocal gravity and comparison with observational datasets},
JCAP \textbf{2014}, 044 (2014).
% Dimensional reduction / fractal spacetime
\bibitem{LauscherReuter2005}
O.~Lauscher, M.~Reuter,
\emph{Fractal spacetime structure in asymptotically safe gravity},
JHEP \textbf{0510}, 050 (2005).
\bibitem{Ambjorn2012}
J.~Ambj{\o}rn et al.,
\emph{Quantum spacetime from dynamical triangulations},
Phys.\ Rept.\ \textbf{519}, 127 (2012).
\bibitem{Carlip2017}
S.~Carlip,
\emph{Spontaneous dimensional reduction in quantum gravity},
Class.\ Quant.\ Grav.\ \textbf{34}, 193001 (2017).
% Holography / emergent gravity connections
\bibitem{Verlinde2017}
E.~Verlinde,
\emph{Emergent Gravity and the Dark Universe},
SciPost Phys.\ \textbf{2}, 016 (2017).
% Cosmology datasets
\bibitem{Scolnic2022}
D.~Scolnic et al.,
\emph{The Pantheon+ Sample},
ApJ \textbf{938}, 113 (2022).
\bibitem{Planck2018}
Planck Collaboration,
\emph{Planck 2018 Results. VI. Cosmological Parameters},
A\&A \textbf{641}, A6 (2020).
% High-z galaxies (JWST)
\bibitem{Labbe2023}
I.~Labb{\'e} et al.,
\emph{A population of candidate massive galaxies at $z=7$--$10$ revealed by JWST},
Nature \textbf{616}, 266 (2023).
\bibitem{Naidu2022}
R.~Naidu et al.,
\emph{Rapid star formation at $z\sim10$},
ApJ \textbf{937}, L25 (2022).
% Galaxy rotation curves
\bibitem{Begeman1989}
K.~Begeman,
\emph{HI rotation curves of spiral galaxies},
A\&A \textbf{223}, 47 (1989).
\bibitem{Bottema1993}
R.~Bottema,
\emph{Disk mass-to-light ratios in spirals},
A\&A \textbf{275}, 16 (1993).
% Reviews of modified gravity
\bibitem{SotiriouFaraoni2010}
T.~Sotiriou, V.~Faraoni,
\emph{$f(R)$ theories of gravity},
Rev.\ Mod.\ Phys.\ \textbf{82}, 451 (2010).
\bibitem{deRham2014}
C.~de Rham,
\emph{Massive Gravity},
Living Rev.\ Rel.\ \textbf{17}, 7 (2014).
% Nonlocal effective action math
\bibitem{Barvinsky1985}
A.~Barvinsky, G.~Vilkovisky,
\emph{The generalized Schwinger-DeWitt technique},
Phys.\ Rept.\ \textbf{119}, 1 (1985).
\bibitem{Dona2015}
P.~Dona, M.~Maggiore, M.~Mancarella,
\emph{Nonlocal gravity with a Weyl-square term},
Phys.\ Rev.\ D \textbf{92}, 084019 (2015).
% Ghost-free infinite-derivative (non-local) gravity
\bibitem{Biswas2012}
T.~Biswas, E.~Gerwick, T.~Koivisto, A.~Mazumdar,
\emph{Towards singularity- and ghost-free theories of gravity},
Phys.\ Rev.\ Lett.\ \textbf{108}, 031101 (2012).
\bibitem{Modesto2012}
L.~Modesto,
\emph{Super-renormalizable quantum gravity},
Phys.\ Rev.\ D \textbf{86}, 044005 (2012).
\bibitem{Tomboulis1997}
E.~T.~Tomboulis,
\emph{Superrenormalizable gauge and gravitational theories},
arXiv:hep-th/9702146 (1997).
\bibitem{Eckert2019}
D.~Eckert, S.~Ettori, V.~Ghirardini et al.,
\emph{Hydrostatic mass profiles in X-COP galaxy clusters},
A\&A \textbf{621}, A39 (2019).
\bibitem{Tian2020}
Y.~Tian, K.~Umetsu, C.-M.~Ko et al.,
\emph{The Radial Acceleration Relation in CLASH Galaxy Clusters},
ApJ \textbf{896}, 70 (2020).
\bibitem{ZhaoFamaey2012}
H.~Zhao, B.~Famaey,
\emph{Comparing different MOND theories: the EMOND proposal},
Phys.\ Rev.\ D \textbf{86}, 067301 (2012).
\bibitem{Deffayet2011}
C.~Deffayet, G.~Esposito-Far\`ese, R.~P.~Woodard,
\emph{Nonlocal metric formulations of MOND with sufficient lensing},
Phys.\ Rev.\ D \textbf{84}, 124054 (2011).
\end{thebibliography}
\newpage
\appendix
% ============================================================
\section{A Jeans-Scale Toy Model of the Fractal Boost (Not a CMB Computation)}
% ============================================================
\label{app:cmb_check}
This appendix illustrates the ``Fractal Boost'' mechanism with a deliberately minimal toy model. \textbf{It is not a CMB computation}: the system solved below omits the photon Boltzmann hierarchy, the Thomson photon--baryon coupling, the metric potentials $\Phi,\Psi$, and neutrinos. The oscillations it produces are \emph{Jeans oscillations} of a self-gravitating baryon fluid, which are a different physical object from the CMB acoustic peaks. The appendix is retained only to show, in the simplest setting, that a scale-dependent $G_{\rm eff}(k)$ can re-amplify pressure-damped oscillations.
\subsection{Methodology}
We evolve the baryon density contrast $\delta_b$ in the sub-horizon regime with a driven-oscillator (modified Jeans) equation:
\begin{equation}
\ddot{\delta}_b + \mathcal{H} \dot{\delta}_b + \left( c_s^2 k^2 - 4\pi G_{\rm eff}(k) \bar{\rho}_b \right) \delta_b = 0
\label{eq:jeans_modified}
\end{equation}
where $G_{\rm eff}(k)$ incorporates a phenomenological FSG-type infrared enhancement.
\subsection{Results}
We integrated this equation from the radiation-dominated era ($a \sim 10^{-4}$) to recombination ($a \sim 10^{-3}$).
As shown in Figure \ref{fig:cmb_check}, the enhanced coupling (red curve) re-amplifies the Jeans oscillations relative to the standard no-DM case (green curve), mimicking at this toy level the sustaining role that Dark Matter potential wells play for the true acoustic oscillations. Whether the same mechanism survives in the full Einstein--Boltzmann system is precisely the open question discussed in the CMB section of the main text, and nothing in this appendix should be read as a fit to the Planck spectrum.
\begin{figure}[h!]
\centering
% Assurez-vous que le fichier s'appelle bien check_cmb_physics.pdf
\includegraphics[width=0.9\textwidth]{check_cmb_physics.pdf}
\caption{
Numerical integration of the modified Jeans equation (toy model; these are Jeans oscillations, not CMB acoustic peaks).
(Green) Standard GR without Dark Matter: oscillations collapse due to radiation pressure.
(Black) $\Lambda$CDM-like reference: an additional matter component sustains them.
(Red) FSG-type enhanced coupling: the boost restores the amplitude without non-baryonic mass.
}
\label{fig:cmb_check}
\end{figure}
\newpage
% ============================================================
\section{Simulation Code (Core Logic)} \label{app:code}
% ============================================================
For reproducibility, we provide the core Python function used to integrate the modified Jeans equation of Appendix~\ref{app:cmb_check} (a toy model, not a Boltzmann solver). The term \texttt{G\_eff\_fsg} implements a phenomenological scale-dependent coupling.
\lstset{language=Python, basicstyle=\ttfamily\footnotesize, breaklines=true, frame=single}
\begin{lstlisting}
import numpy as np
def G_eff_fsg(k, a):
    """
    FSG Model: Effective Gravity runs with scale k.
    k_trans is set by the cosmic horizon scale.
    """
    k_trans = 0.05 
    
    # Fractal Boost: G_eff increases in the IR (small k)
    # The exponent 1.5 is phenomenological in this effective model
    term = (k_trans / k)**1.5
    boost = 1.0 + 0.1 * term
    
    # Saturation to avoid numerical singularities at k->0
    return np.minimum(boost, 50.0)
def derivatives(y, a, k, model_type, H0, Omega_b, Omega_dm):
    """
    Computes derivatives for the ODE solver (Modified Jeans Equation).
    y[0] = delta (density contrast)
    y[1] = d_delta/da
    """
    delta = y[0]
    d_delta = y[1]
    H_val = hubble(a) # User-defined Hubble function
    
    # Friction term (Expansion)
    friction = (3 / (2 * a)) * d_delta
    
    # Pressure term (Acoustic Oscillations)
    cs2 = 1.0/3.0 # Relativistic sound speed
    # Note: H0 must be in compatible units
    pressure = (cs2 * k**2 / (a**2 * H0**2)) * delta
    
    # Gravity Source Term
    if model_type == "FSG":
        # Only Baryons, but with Boosted Gravity (Fractal)
        density = Omega_b / a**3
        G_fac = G_eff_fsg(k, a)
    else:
        # Standard Case (GR)
        density = (Omega_b + Omega_dm) / a**3
        G_fac = 1.0
    
    # Source term proportional to 4*pi*G*rho
    # Factor 1.5 comes from 4*pi*G*rho_crit / H^2 = 1.5 * Omega
    gravity = 1.5 * (density / (H_val/H0)**2) * delta * G_fac
    # Second order ODE: delta'' + friction + pressure - gravity = 0
    d2_delta = gravity - pressure - friction
    
    return [d_delta, d2_delta]
\end{lstlisting}
\end{onehalfspacing}
\end{document}

